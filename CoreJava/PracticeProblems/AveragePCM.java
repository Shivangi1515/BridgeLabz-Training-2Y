public class AveragePCM {
    public static void main(String[] args) {
        // Marks in Maths, Physics, and Chemistry
        int maths = 94;
        int physics = 95;
        int chemistry = 96;

        // Calculate total
        int total = maths + physics + chemistry;

        // Calculate average
        double average = total / 3.0; // Use 3.0 to get decimal if needed

        // Output
        System.out.println("Sam’s average mark in PCM is " + average);
    }
}
